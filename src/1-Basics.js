// Known Code v1

// Lesson One Data Types and how we work with them.
// Basic Types

// Variables
let name = 'Luis';

// Constant
const freezingFarenheit = 32;

// Array of values
const setOfNames = [name, 'Zoey', 'Clover'];

// Object
let character = {
  name: 'Mario',
  buddy: 'Luigi',
  favoriteLunch: 'Pasta',
  occupation: 'Plumber'
};

// Boolean
let isActive = true;

// Null Value
let unknownValue = null;

// // Function
// function addNumbers(x, y) {
//   return x + y;
// }

// Adding two strings
// console.log(name + ' ' + name);

/// Operations
// Addition of Numbers
let b = 3;
let c = 5;
const a = b + c;
console.log(a);

// Adding Number and String
const x = freezingFarenheit + '32';
console.log(x);

const y = freezingFarenheit + freezingFarenheit;

// Subtraction
console.log(4 - 2);

// Multiply
console.log(5 * 25);

// Divide
console.log(100 / 10);
